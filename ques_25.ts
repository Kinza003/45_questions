/* Alien Colors #1: Imagine an alien was just 
   shot down in a game. Assign 'green', 'yellow', 
   or 'red' to a variable called alien_color.
· Write an if-else statement to test whether the 
alien's color is green. If is it, print a message 
that the player just earned 5 points. 
· Write one version of this program that passes the 
if test and another that fails. (the version that fails have no output) */

let alien_color: string = "green";

if (alien_color === "green") {
  console.log("Congragulations! you have earned 5 points.");
} else {
  // no output because the condition is false
}
